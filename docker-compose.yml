services:
  league-trader-temple:
    build:
      context: .
      dockerfile: League_Trader_Temple.Server/Dockerfile
    image: league-trader-temple:latest
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_HTTP_PORTS: 8080
